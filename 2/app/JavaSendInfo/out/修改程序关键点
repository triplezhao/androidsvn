1.添加渠道时候可以不用修改程序。
2.如果添加的渠道对机型有要求就要修改一下地方。
（1）reqbean.java
	//这个方法是随机某一渠道里面的机型
	private  String getRandomDevice_model(){
		else if(channel.equals("TCL")){
			
		}
	}
	
3.
1.如果新添加渠道，则直接在参数中写新渠道名字，程序会自动为渠道新建一个tb_渠道名的表。
2.需要将以前的一个表中的n个渠道，数据导出到新表中，名字为tb_渠道名。
	
